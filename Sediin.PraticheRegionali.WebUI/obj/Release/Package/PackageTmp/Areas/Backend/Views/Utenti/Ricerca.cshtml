@model Sediin.PraticheRegionali.WebUI.Areas.Backend.Models.UtentiRicercaModel

<div class="row mb-4">
    <div class="col-md-12">


        <div class="btn-group">
            <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="true">
                Nuovo Utente
            </button>
            <ul class="dropdown-menu" style="position: absolute; inset: 0px auto auto 0px; margin: 0px; transform: translate(0px, 40px);" data-popper-placement="bottom-start">

                @foreach (var item in Html.GetRolesFriendlyName()
                        .Where(z => z.Attivo == true && z.InsertAdmin == true).OrderBy(o => o.FriendlyName))
                {


                    <li>
                        @*<a class="dropdown-item fw-bold" data-ajax="true" data-ajax-begin="alertWaid()"
                               data-ajax-failure="handleError"
                               data-ajax-success="showModal_NoFooter('Nuovo Utente Admin',data); alertClose()"
                               href="@Html.EncodedAction("UtenteNuovo", "Utenti", new { ruolo = item.Rolename})">
                               Nuovo Utente "@item.FriendlyName"
                               <br><small class="fw-normal">@item.Descrizione</small>
                        </a>*@
                        <a class="dropdown-item fw-bold" data-ajax="true" data-ajax-begin="alertWaid()"
                               data-ajax-failure="handleError"
                               data-ajax-success="showModal_NoFooter('Nuovo Utente @item.FriendlyName',data); alertClose()"
                               href="@Html.EncodedAction("UtenteNuovo", "Utenti", new { ruolo = item.Rolename})">
                               Nuovo Utente "@item.FriendlyName"
                               <br><small class="fw-normal">@item.Descrizione</small>
                        </a>

                    </li>
                }
      
            </ul>
        </div>
   </div>
</div>
@Html.RicercaModulo(Html.Partial("RicercaModulo", Model), "Utenti registrati")

@Html.UpdateListRicerca(Url.Action("Ricerca", "Utenti"), true)

